# Spring Boot + Thymeleaf + Spring Security + JPA Assignment

# Dump for the Database is in the file named "radiologyDump.sql", you can import it to your MySQL database to have the same data as the one used in the project.

# Website is running on port 8080, you can access it by the following URL:
HomePage: http://localhost:8080/

